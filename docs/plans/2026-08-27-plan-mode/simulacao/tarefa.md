Quero adicionar ao Codex (este repositório, fork em codex-rs/) persistência de planos do Plan mode: cada plano final (o bloco `<proposed_plan>`) deve ser guardado como ficheiro markdown em `~/.codex/plans/`, e quero um comando `/plans` no TUI que liste os planos anteriores e permita carregar um deles como contexto na sessão atual para implementação. O Desktop (via app-server) também deve conseguir listar e carregar planos. Faz o plano de implementação.
